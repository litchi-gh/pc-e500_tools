{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Pocket BASIC / ASM Linter",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/examples/sample.e500",
        "${workspaceFolder}/examples/sample.asm"
      ],
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "preLaunchTask": "npm: compile"
    }
  ]
}
